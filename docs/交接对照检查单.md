# exchange 交接包对照检查单

- 对照对象：`D:\PythonProjects\exchange\`（交接日期 2026-08-25）与本项目 `src/ai_penetration`
- 检查日期：2026-08-27
- 结论分级：✅ 符合 · ⚠️ 口径差异（已有实现但与交接口径不一致）· ❌ 未完成 · 🔶 超出交接当前阶段

## 一、总体判断

交接说明第二节的四个冻结前置步骤中，本项目只完成了前两步（频数计算、歧义检查表），
**步骤 3（阈值激活）与步骤 4（重新生成正式 A 级词典 + QC 报告）尚未实现**；
第四节研究设计已确定口径（锚点词表、三种窗口、判定阈值、描述去重、字段命名、
交付物清单）与现有实现存在系统性差异。词典入库环节行数/QC 校验值与交接包完全一致。

## 二、逐项对照

### 词典入库（对应 import_exchange_dict.py）

| 项 | 交接口径 | 本项目 | 状态 |
|---|---|---|---|
| 概念行数 / 唯一 skill_id | 22,683 / 22,683 | EXPECTED_CONCEPTS=22683，校验 count=count(DISTINCT) | ✅ |
| 别名总行数 | 153,804 | EXPECTED_ALIASES=153804 | ✅ |
| 激活别名数 | 107,467（新增激活 0） | 校验 act==107467 | ✅ |
| CSV 字段清单 | 概念表 37 列、别名表 17 列 | CREATE TABLE 逐列一致，全 text | ✅ |
| 版本识别 | bilingual_complete_candidate_v1.0，状态 complete_candidate_not_frozen | 表内保留 dictionary_version/translation_status 字段 | ✅（字段在，未做状态拦截） |
| 入库前 SHA-256 自校验 | 交接包含全文件哈希清单 | 未校验 CSV 哈希 | ❌ 小缺口 |
| ai_dict schema 命名 | 交接包无此约定（自建不违规） | ai_dict.skill_concepts/skill_aliases | ✅ 说明 |

### 冻结前置步骤（交接说明第二节，逐字四步）

| 步骤 | 本项目 | 状态 |
|---|---|---|
| 1. 计算新增中文/中英混合别名的不同岗位描述频数 | `zh_alias_freq.py` → `ai_dict.zh_alias_freq` | ⚠️ 口径差异，见下 |
| 2. 一对多映射、过短词、跨技能歧义检查 | `ai_dict.alias_ambiguity`（n_skills>1 或长度≤2） | ✅ 基本对应 |
| 3. 仅激活满足出现阈值且通过歧义检查的新增别名 | **无任何代码** | ❌ 未实现 |
| 4. 重新生成正式外部 A 级双语词典及 QC 报告 | **无任何代码** | ❌ 未实现 |

步骤 1 的口径差异（对照指南 §10.3.2 逐字定义）：
- 交接要求「不同**规范化**岗位描述文本数量，以 `COUNT(DISTINCT text_hash)` 计算，
  不受相同文本重复发布次数影响」；本项目按**行**计数（岗位内去重仅限同一条描述内，
  相同描述的不同发布会计为多个 distinct），与去重主规则不一致。
- 语料覆盖：本项目默认只扫广深 2 市（且此前默认仅 2024 一年）；交接口径为
  中国招聘语料、样本年份 2014–2025。覆盖不足。
- **数据陈旧**：`ai_dict.zh_alias_freq` 现有内容是大小写匹配 bug 修复前写入的，
  含大写字母的别名频数被错记，且计数口径不符——修复后必须整表重算。
- 阈值数值：交接包**未规定**中文别名激活的具体频数阈值（风险点，需自行确定
  并记录依据；包内唯一量化的文档频数是 B 级 `df_unique_description>=100`，
  属技能分级而非别名激活，不可直接混用）。

### 研究设计口径（交接说明第三节 + 指南）

| 项 | 交接口径 | 本项目 | 状态 |
|---|---|---|---|
| 样本年份 | 2014–2025 | 全部入口默认/硬编码到 2024 | ⚠️ |
| 主锚点 | AI、机器学习、NLP、计算机视觉 + 严格限定大语言模型/LLM/Transformer（禁裸"大模型"、裸 Transformer；Transformer 须与模型/架构类词共现；另设 anchor_cn_paper/anchor_babina 对照口径；锚点正则须有含反例的单测） | `AI_ANCHOR_SKILLS`=8 词（含深度学习/图像识别/语音识别/强化学习/神经网络，缺 AI 与严格 LLM/Transformer 组） | ⚠️ 系统性差异 |
| 共现率窗口 | 9 组 = 3 锚点 × {annual, pooled 全期, roll3_centered 居中三年滚动（边界年 2 年窗）} | 仅 annual | ❌ 缺 pooled/滚动 |
| 权重平滑 | Beta-Binomial 经验贝叶斯（§14.2），不覆盖原始权重 | 无 | ❌ |
| 岗位 AI 判定 | 主口径 AIscore **>** 0.05（严格大于），另存 0.10/0.15，单调性检查 | anchor 阈值默认 0.3 且用 ≥；fusion 用 ≥0.15 且 max≥0.5 | ⚠️ 阈值与比较符均不同 |
| 描述去重 | 同平台描述完全相同的广告合并（§6.2 主规则+近似去重稳健版） | 全量扫描无去重 | ⚠️ |
| 无技能岗位 | 保留、matched_skill_count=0、得分记**缺失**非 0、阈值标识设 0 | 未区分缺失与 0 | ❌ |
| 岗位相关度 | 岗位内去重技能共现率简单平均 | `job_ai_relevance_anchor` 即均值 | ✅ |
| 字段命名 | `aiscore_<anchor>_<window>_<scoretype>` / `aijob_..._<threshold>`，禁用通用名 AIscore/AIjob 作最终列 | total/ai_jobs/ai_rate 等 | ⚠️ |
| 计数不变量/发布检查单 | §13.7、§17.6 十项 100% 通过否则阻断 | 无对应实现 | ❌ |
| 交付物 | 11 件 parquet + 同名 .metadata.json + quality_control_report.md | CSV/MD 轻量报告 | ❌ 未达发布形态 |
| 企业—年度指标 | **当前阶段明确不计算** | `company_airatio.py` 已实现（论文方向） | 🔶 提前于交接阶段；作后续阶段隔离，勿混入主结果 |
| 翻译执行器 | 170/170 已校验，禁止重跑 | 未触碰 | ✅ |
| LLM 挖掘规范（若做 B/C/D 级发现） | §8：固定推理参数（temperature=0、seed=20260822、guided_json）、Schema/证据跨度绑定（text[start:end]==surface）、映射仅限现有 ID/NEW_CONCEPT/AMBIGUOUS、通过线（Schema 有效率≥99.5% 等 5 项）、缓存键纪律 | `llm_skill_mining`/`llm_alias_generation` 为轻量实现，无证据绑定/Schema 校验/seed/通过线统计 | ⚠️ 不满足 §8（进入该阶段前须改造） |

## 三、建议补齐顺序（对齐交接说明第五节）

1. **修复并重跑 `zh_alias_freq`**：先按 `COUNT(DISTINCT normalized_hash)` 去重口径
   改写计数逻辑，扩展到 21 市 × 2014–2025（或先定义语料版本），重算整表。
2. **确定激活阈值并实现步骤 3**：写 `zh_alias_activation.py`（按频数 +
   ambiguity 表执行 UPDATE is_active='1'，留 run_manifest + 变更行数报告）；
   阈值取值依据写入文档（交接口包未定，属接收方裁量，需说明）。
3. **实现步骤 4**：重新导出正式 A 级双语词典 CSV + QC 报告（行数、哈希、
   激活分布），发布状态从 not_frozen 推进。
4. **共现率管线对表指南口径**：锚点词表重定义（严格 LLM/Transformer 组 +
   两套对照锚点 + 反例单测）、pooled/roll3 窗口、Beta-Binomial 平滑、
   三阈值判定与不变量检查、字段命名改造、零技能岗位处理。
5. **企业 AIRatio 隔离声明**：在项目文档中注明其属交接"当前阶段"之外的
   论文扩展，结论不回填技能共现主结果。
6. 入库脚本补 SHA-256 自校验（小项，随手做）。

## 四、遗留风险与待用户决策

- 中文别名激活频数阈值：数值需你定（建议参考 B 级 100 的下限量级，
  结合 zh_alias_freq 重算后的分布分位决定）。
- eps 是否含 2025 年数据：交接口径 2014–2025，若库内无 2025 需向交接方确认。
- 是否把「渗透率管线」全面对齐指南 §18 的 11 件 parquet 发布形态，
  还是维持研究探索期轻量 CSV——取决于本阶段目标。
